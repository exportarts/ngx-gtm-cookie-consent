sonar.projectKey=exportarts_ngx-gtm-cookie-consent
sonar.organization=exportarts

# this is the name and version displayed in the SonarCloud UI.
sonar.projectName=ngx-gtm-cookie-consent
sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# This property is optional if sonar.modules is set. 
sonar.sources=projects
 
sonar.pullrequest.github.repository=exportarts/ngx-gtm-cookie-consent
